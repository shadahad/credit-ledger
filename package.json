{
  "name": "credit-ledger",
  "version": "1.0.0",
  "description": "Financial ledger for user credits with job lifecycle isolation",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "test": "node --test --test-concurrency=1 --test-reporter=tap tests/**/*.test.js",
    "db:setup": "node db/setup.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  }
}